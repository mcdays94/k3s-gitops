# Prometheus and Grafana now use local-path storage (storageClassName defined in Helm values).
# This file is kept for reference but no longer creates NFS PersistentVolumes.
#
# Previous NFS PVs for prometheus and grafana on 10.10.10.70 have been removed.
# Storage is now dynamically provisioned by the local-path-provisioner.
