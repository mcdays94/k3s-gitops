# Grafana PVC is now managed by the kube-prometheus-stack Helm chart
# using local-path storageClass. This file is kept for reference.
#
# Previous manually-created PVC with NFS selector has been removed.
